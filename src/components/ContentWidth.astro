---
// Constrains content to a consistent max-width across header, main, and footer

interface Props {
	extraClasses?: string;
}

const { extraClasses } = Astro.props;
---

<div class:list={['container mx-auto max-w-4xl px-4', extraClasses]}>
	<slot />
</div>
